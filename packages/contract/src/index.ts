/**
 * @gba-studio/contract — the exec/file adapter contract (SYSTEM_PLAN §1.9).
 * The keystone every adapter and agent tool depends on. Types + pure helpers,
 * zero I/O, zero runtime deps.
 */

export * from './types.js';
export * from './paths.js';
export * from './adapter.js';
